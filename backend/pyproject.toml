[project]
name = "backend"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "django>=6.0.5",
    "django-ninja>=1.4",
    "pillow>=11.0",
    "python-dotenv>=1.0",
    "qrcode[pil]>=8.0",
]
