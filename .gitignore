# OS / éditeurs
.DS_Store
Thumbs.db
.idea/
.vscode/
.claude/

# Node (frontend)
node_modules/
dist/

# Python (backend) — voir aussi backend/.gitignore
__pycache__/
*.py[oc]
.venv/

# Cahiers des charges (documents de travail, jamais committés)
CAHIER_DES_CHARGES*.md
cahier_des_charges*.txt

# ── Données personnelles (CNI, listes de participants) — NE JAMAIS committer ──
# Photos de cartes d'identité, listes nominatives, CSV de contrôle, PDF générés.
photos_*/
Photos_*/
PDFs_*/
*.xlsx
controle_cni*.csv
liste_*.csv

# Notebook scratch vide
/test.ipynb

# Déploiement — état local au serveur, jamais committé
deploy/.last_deploy_sha
deploy/.deploy.lock
